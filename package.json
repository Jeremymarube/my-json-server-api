{
  "name": "json-server-api",
  "version": "1.0.0",
  "description": "JSON Server API for my projects",
  "main": "server.js",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT db.json",
    "dev": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}